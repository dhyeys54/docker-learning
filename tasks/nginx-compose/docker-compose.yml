version: '1.0'
services:
  web:
    image: nginx:latest
    ports:
      - 80:80
    volumes:
      - ./tasks/html-files:/usr/share/nginx/html

  pg:
    image: postgres:alpine
    # environment:
      # - POSTGRES_DB=test
    env_file:
      - ./db-test1.env